(()=>{
  const KEY='jr_bom_stage_flows_v30';
  const uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,7);
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const read=()=>{try{return JSON.parse(localStorage.getItem(KEY))||[]}catch(_){return[]}};
  const seed={id:'sb042e2',title:'PASSIVE BRACKET ASSEMBLY',finalPart:'SB-042E-2',stages:[
    {id:uid(),process:'PRESS / STAMP ASSEMBLY',detail:'ปั๊มประกอบ LOWER BASE + BOLT',outputPart:'SB-042E-2-2',outputName:'PASSIVE BRACKET ASSEMBLY',changed:true,inputs:[
      {id:uid(),partNo:'SB-042B-2-2-1',name:'LOWER BASE',source:'In-house',processes:[{name:'Stamping',steps:1}]},
      {id:uid(),partNo:'SB-042B-2-2-2',name:'BOLT',source:'Supplier',processes:[]}
    ]},
    {id:uid(),process:'WELDING',detail:'เชื่อม SB-042E-2-2 กับ SB-042C-2-1',outputPart:'SB-042E-2-2',outputName:'WELDED ASSEMBLY',changed:false,inputs:[
      {id:uid(),partNo:'SB-042C-2-1',name:'LOWER PLATE',source:'In-house',processes:[{name:'Stamping',steps:1}]}
    ]},
    {id:uid(),process:'RUBBER CAP ASSEMBLY',detail:'ใส่ RUBBER CAP',outputPart:'SB-042E-2',outputName:'FINAL PASSIVE BRACKET ASSEMBLY',changed:true,inputs:[
      {id:uid(),partNo:'SB-042B-2-2-3',name:'RUBBER CAP',source:'Supplier',processes:[]}
    ]}
  ]};
  let flows=read();if(!flows.length)flows=[seed];let selected=flows[0]?.id,editingStage=null,tempInputs=[];
  const save=()=>localStorage.setItem(KEY,JSON.stringify(flows));
  const view=document.getElementById('view-bom');if(!view)return;
  const style=document.createElement('style');style.textContent=`
    .bf-head{display:flex;justify-content:space-between;gap:14px;align-items:end;margin-bottom:16px}.bf-actions{display:flex;gap:8px;flex-wrap:wrap}.bf-flow{padding:20px;overflow:auto}.bf-title{text-align:center;color:#12345b;margin-bottom:22px}.bf-title h2{font-size:27px;margin:0}.bf-title p{font-size:17px;font-weight:850;margin:4px 0}.bf-stage{display:grid;grid-template-columns:minmax(230px,1fr) 42px minmax(210px,.8fr) 42px minmax(230px,1fr);align-items:center;gap:8px;margin:0 auto 22px;min-width:920px;position:relative}.bf-inputs{display:grid;gap:9px}.bf-part,.bf-output{border:2px solid #0870c9;border-radius:9px;background:#fff;overflow:hidden;box-shadow:0 4px 12px rgba(15,23,42,.09)}.bf-part{cursor:pointer}.bf-part-head,.bf-output-head{padding:7px 9px;background:#075aa5;color:#fff;text-align:center;font-weight:900;line-height:1.2}.bf-part-body{display:grid;grid-template-columns:62px 1fr;gap:9px;align-items:center;padding:9px}.bf-part-img{width:62px;height:54px;object-fit:cover;border-radius:6px;background:#e2e8f0}.bf-part-meta{font-size:11px;color:#475569}.bf-source{display:inline-block;margin-top:5px;padding:3px 7px;border-radius:999px;font-size:9px;font-weight:900}.bf-source.in{background:#dbeafe;color:#075aa5}.bf-source.sup{background:#fef3c7;color:#92400e}.bf-arrow{font-size:34px;color:#0870c9;text-align:center;font-weight:950}.bf-process{border:2px solid #f97316;border-radius:9px;background:#fff7ed;text-align:center;overflow:hidden}.bf-process strong{display:block;padding:12px 8px;background:#f97316;color:#fff;font-size:16px}.bf-process span{display:block;padding:9px;font-size:11px}.bf-output{border-color:#19923a}.bf-output-head{background:#168333}.bf-output-body{padding:14px;text-align:center;font-weight:950}.bf-change{display:block;margin-bottom:5px;color:#fff;background:#11752c;font-size:10px;padding:3px}.bf-same{background:#64748b}.bf-stage-tools{position:absolute;right:0;bottom:-20px;display:flex;gap:5px}.bf-stage-tools button{border:0;border-radius:6px;padding:4px 7px;font-size:9px;cursor:pointer}.bf-add-next{display:block;margin:8px auto 20px}.bf-empty{text-align:center;padding:50px;color:#64748b}.bf-editor{padding:18px;margin-bottom:16px}.bf-editor[hidden],.bf-detail[hidden]{display:none}.bf-form-grid{display:grid;grid-template-columns:repeat(2,minmax(220px,1fr));gap:12px}.bf-section{grid-column:1/-1;padding:11px 13px;border-radius:9px;background:#eff6ff;color:#075aa5;font-weight:900}.bf-component-box{grid-column:1/-1;border:1px solid #dbe3ee;border-radius:10px;padding:13px}.bf-component-grid{display:grid;grid-template-columns:repeat(3,minmax(140px,1fr));gap:9px}.bf-temp-list{display:grid;gap:7px;margin-top:10px}.bf-temp-item{display:flex;justify-content:space-between;gap:10px;padding:8px 10px;background:#f8fafc;border-radius:8px;font-size:11px}.bf-detail{position:fixed;inset:0;background:rgba(15,23,42,.45);z-index:100;display:grid;place-items:center;padding:20px}.bf-detail-card{width:min(440px,100%);max-height:80vh;overflow:auto;background:#fff;border-radius:14px;padding:20px}.bf-process-row{display:flex;justify-content:space-between;padding:10px;margin-top:7px;border-radius:8px;background:#eff6ff}.bf-delete{background:#fee2e2;color:#b91c1c}.bf-edit{background:#dbeafe;color:#1d4ed8}@media(max-width:700px){.bf-head{display:grid}.bf-form-grid,.bf-component-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(style);
  style.textContent+=`.bf-canvas-wrap{overflow:auto;border:1px solid #dbe3ee;border-radius:14px;background:#f8fbff}.bf-canvas{position:relative;min-width:1000px;min-height:680px;background-image:linear-gradient(#edf2f7 1px,transparent 1px),linear-gradient(90deg,#edf2f7 1px,transparent 1px);background-size:24px 24px}.bf-canvas svg{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:1}.bf-canvas .bf-part,.bf-canvas .bf-process,.bf-canvas .bf-output{position:absolute;width:220px;z-index:3;cursor:move;user-select:none}.bf-canvas .bf-process{width:240px}.bf-canvas .bf-output{width:240px}.bf-canvas-hint{position:absolute;left:12px;top:10px;z-index:4;padding:7px 10px;border-radius:8px;background:#fff;color:#075aa5;font-size:11px;font-weight:900}.bf-node-tools{display:flex;justify-content:center;gap:5px;padding:5px;background:#fff}.bf-node-tools button{border:0;border-radius:5px;padding:3px 6px;font-size:9px;cursor:pointer}.bf-flow-line{stroke:#0870c9;stroke-width:5;fill:none}.bf-part.dragging,.bf-process.dragging,.bf-output.dragging{opacity:.7;box-shadow:0 14px 30px rgba(2,95,155,.3)}`;
  view.innerHTML=`<div class="bf-head card" style="padding:15px"><div><h2>PART BOM & PROCESS FLOW</h2><p>สร้างผังทีละขั้นตอนการประกอบ</p></div><div class="bf-actions"><select id="bfSelect"></select><button class="btn ghost" id="bfNew">+ สร้าง BOM ใหม่</button><button class="btn primary" id="bfAdd">+ เพิ่มขั้นตอนถัดไป</button></div></div>
    <article class="card bf-editor" id="bfEditor" hidden><div class="panel-head"><div><h2 id="bfEditorTitle">เพิ่มขั้นตอน</h2><p>กรอกตามลำดับ 1 → 2 → 3</p></div><button class="btn ghost" id="bfClose">ปิด</button></div><form id="bfForm"><div class="bf-form-grid">
      <div class="bf-section">1. Process ที่จะทำ</div><div class="field"><label>ชื่อ Process *</label><input id="bfProcess" required placeholder="เช่น WELDING"></div><div class="field"><label>รายละเอียดงาน</label><input id="bfDetailText" placeholder="เช่น เชื่อม Part A กับ Part B"></div>
      <div class="bf-section">2. Part ที่ได้หลังจบ Process</div><div class="field"><label>Part No. หลัง Process *</label><input id="bfOutput" required></div><div class="field"><label>ชื่อ Part</label><input id="bfOutputName"></div><label><input type="checkbox" id="bfChanged"> Part No. เปลี่ยนหลัง Process นี้</label>
      <div class="bf-section">3. เพิ่ม Part / Component ที่นำมาประกอบ</div><div class="bf-component-box"><div class="bf-component-grid"><div class="field"><label>Part No.</label><input id="bfPartNo"></div><div class="field"><label>ชื่อ Part</label><input id="bfPartName"></div><div class="field"><label>แหล่งที่มา</label><select id="bfSource"><option value="In-house">ผลิตเอง (In-house)</option><option value="Supplier">รับจาก Supplier</option></select></div><div class="field"><label>Process ที่ผ่านมาก่อนหน้า</label><input id="bfPrevProcess" placeholder="เช่น Stamping"></div><div class="field"><label>จำนวน Step</label><input id="bfSteps" type="number" min="1" value="1"></div><div class="field"><label>&nbsp;</label><button class="btn ghost" type="button" id="bfAddPart">+ เพิ่ม Component</button></div></div><div class="bf-temp-list" id="bfTempList"></div></div>
      <div class="bf-actions"><button class="btn primary" type="submit">บันทึกขั้นตอน</button><button class="btn ghost" type="button" id="bfCancel">ยกเลิก</button></div>
    </div></form></article><article class="card bf-flow" id="bfFlow"></article><div class="bf-detail" id="bfDetail" hidden></div>`;
  const $=id=>document.getElementById(id),flow=()=>flows.find(f=>f.id===selected);
  const imageFor=p=>{try{return localStorage.getItem('jr_bom_image_'+p)||''}catch(_){return''}};
  const refreshSelect=()=>{$('bfSelect').innerHTML=flows.map(f=>`<option value="${f.id}">${esc(f.finalPart)} — ${esc(f.title)}</option>`).join('');$('bfSelect').value=selected};
  const partCard=p=>{const img=imageFor(p.partNo);return `<div class="bf-part" data-part="${p.id}"><div class="bf-part-head">${esc(p.partNo)}<br><small>${esc(p.name||'COMPONENT')}</small></div><div class="bf-part-body">${img?`<img class="bf-part-img" src="${img}">`:'<div class="bf-part-img"></div>'}<div class="bf-part-meta">${(p.processes||[]).map(x=>`${esc(x.name)} ${x.steps} Step`).join('<br>')||'คลิกเพื่อดูรายละเอียด'}<br><span class="bf-source ${p.source==='Supplier'?'sup':'in'}">${p.source==='Supplier'?'SUPPLIER':'IN-HOUSE'}</span></div></div></div>`};
  let graphEdges=[];
  const nodePos=(f,key,fallback)=>{f.layout=f.layout||{};return f.layout[key]||fallback};
  const render=()=>{const f=flow();refreshSelect();if(!f){$('bfFlow').innerHTML='<div class="bf-empty">กด “สร้าง BOM ใหม่” เพื่อเริ่มต้น</div>';return}let y=70,previousOutput='',inputCounter=0;graphEdges=[];const nodes=[];f.stages.forEach((s,si)=>{const stageHeight=Math.max(210,(s.inputs||[]).length*135),processKey=`process-${s.id}`,outputKey=`output-${s.id}`,pp=nodePos(f,processKey,{x:390,y:y+Math.max(20,(stageHeight-105)/2)}),op=nodePos(f,outputKey,{x:730,y:y+Math.max(15,(stageHeight-125)/2)});(s.inputs||[]).forEach((p,pi)=>{const key=`part-${s.id}-${p.id}`,pos=nodePos(f,key,{x:25,y:y+pi*135}),img=imageFor(p.partNo),meta=(p.processes||[]).map(x=>`${esc(x.name)} ${x.steps} Step`).join('<br>')||'คลิกเพื่อดูรายละเอียด';nodes.push(`<div class="bf-part" data-node="${key}" data-part="${p.id}" style="left:${pos.x}px;top:${pos.y}px"><div class="bf-part-head">${esc(p.partNo)}<br><small>${esc(p.name||'COMPONENT')}</small></div><div class="bf-part-body">${img?`<img class="bf-part-img" src="${img}">`:'<div class="bf-part-img"></div>'}<div class="bf-part-meta">${meta}<br><span class="bf-source ${p.source==='Supplier'?'sup':'in'}">${p.source==='Supplier'?'SUPPLIER':'IN-HOUSE'}</span></div></div></div>`);graphEdges.push([key,processKey]);inputCounter++});nodes.push(`<div class="bf-process" data-node="${processKey}" style="left:${pp.x}px;top:${pp.y}px"><strong>${esc(s.process)}</strong><span>${esc(s.detail||'-')}</span><div class="bf-node-tools"><button class="bf-edit" data-edit-stage="${s.id}">แก้ไข</button><button class="bf-delete" data-delete-stage="${s.id}">ลบ</button></div></div>`);nodes.push(`<div class="bf-output" data-node="${outputKey}" style="left:${op.x}px;top:${op.y}px"><div class="bf-output-head"><span class="bf-change ${s.changed?'':'bf-same'}">${s.changed?'PART NO. CHANGED':'PART NO. NOT CHANGED'}</span>${esc(s.outputPart)}</div><div class="bf-output-body">${esc(s.outputName||'OUTPUT PART')}</div></div>`);graphEdges.push([processKey,outputKey]);if(previousOutput)graphEdges.push([previousOutput,processKey]);previousOutput=outputKey;y+=stageHeight+65});const height=Math.max(680,y+40);$('bfFlow').innerHTML=`<div class="bf-title"><h2>PART BOM & PROCESS FLOW</h2><p>${esc(f.title)} (${esc(f.finalPart)})</p></div><div class="bf-canvas-wrap"><div class="bf-canvas" style="height:${height}px"><div class="bf-canvas-hint">ลากทุกกล่องไปวางได้อิสระ · เส้นจะขยับตามอัตโนมัติ</div><svg><defs><marker id="bfArrow" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto"><path d="M0 0 L0 6 L9 3 Z" fill="#0870c9"></path></marker></defs></svg>${nodes.join('')}</div></div><button class="btn primary bf-add-next" id="bfAddBottom">+ เพิ่มขั้นตอนถัดไป</button>`;$('bfAddBottom').onclick=()=>openEditor();requestAnimationFrame(drawGraph)};
  const drawGraph=()=>{const canvas=document.querySelector('.bf-canvas'),svg=canvas?.querySelector('svg');if(!canvas||!svg)return;svg.querySelectorAll('.bf-flow-line').forEach(x=>x.remove());const cr=canvas.getBoundingClientRect();graphEdges.forEach(([from,to])=>{const a=canvas.querySelector(`[data-node="${CSS.escape(from)}"]`),b=canvas.querySelector(`[data-node="${CSS.escape(to)}"]`);if(!a||!b)return;const ar=a.getBoundingClientRect(),br=b.getBoundingClientRect(),x1=ar.right-cr.left,y1=ar.top-cr.top+ar.height/2,x2=br.left-cr.left,y2=br.top-cr.top+br.height/2,p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('class','bf-flow-line');const mid=(x1+x2)/2;p.setAttribute('d',`M${x1},${y1} C${mid},${y1} ${mid},${y2} ${x2},${y2}`);p.setAttribute('marker-end','url(#bfArrow)');svg.appendChild(p)})};
  const renderTemp=()=>{$('bfTempList').innerHTML=tempInputs.map((p,i)=>`<div class="bf-temp-item"><span><strong>${esc(p.partNo)}</strong> · ${esc(p.name)} · ${p.source==='Supplier'?'Supplier':'In-house'} · ${(p.processes||[]).map(x=>`${esc(x.name)} ${x.steps} Step`).join(', ')||'ไม่ระบุ Process'}</span><button type="button" data-remove-temp="${i}">ลบ</button></div>`).join('')};
  const openEditor=id=>{editingStage=id||null;const s=id?flow().stages.find(x=>x.id===id):null,tempInputs=s?JSON.parse(JSON.stringify(s.inputs||[])):[];$('bfForm').reset();$('bfSteps').value=1;$('bfEditorTitle').textContent=s?'แก้ไขขั้นตอน':'เพิ่มขั้นตอนถัดไป';if(s){$('bfProcess').value=s.process;$('bfDetailText').value=s.detail||'';$('bfOutput').value=s.outputPart;$('bfOutputName').value=s.outputName||'';$('bfChanged').checked=!!s.changed}else{const stages=flow()?.stages||[];$('bfOutput').value=stages.at(-1)?.outputPart||''}renderTemp();$('bfEditor').hidden=false;$('bfEditor').scrollIntoView({behavior:'smooth'})};
  $('bfAddPart').onclick=()=>{const partNo=$('bfPartNo').value.trim();if(!partNo)return showToast('กรุณากรอก Part No.');const proc=$('bfPrevProcess').value.trim();tempInputs.push({id:uid(),partNo,name:$('bfPartName').value.trim(),source:$('bfSource').value,processes:proc?[{name:proc,steps:Math.max(1,Number($('bfSteps').value)||1)}]:[]});['bfPartNo','bfPartName','bfPrevProcess'].forEach(id=>$(id).value='');$('bfSteps').value=1;renderTemp()};
  $('bfTempList').onclick=e=>{const b=e.target.closest('[data-remove-temp]');if(!b)return;tempInputs.splice(Number(b.dataset.removeTemp),1);renderTemp()};
  $('bfForm').onsubmit=e=>{e.preventDefault();const s={id:editingStage||uid(),process:$('bfProcess').value.trim(),detail:$('bfDetailText').value.trim(),outputPart:$('bfOutput').value.trim(),outputName:$('bfOutputName').value.trim(),changed:$('bfChanged').checked,inputs:tempInputs};const f=flow();if(editingStage){const i=f.stages.findIndex(x=>x.id===editingStage);f.stages[i]=s}else f.stages.push(s);save();$('bfEditor').hidden=true;render();showToast('บันทึกขั้นตอนแล้ว')};
  $('bfNew').onclick=()=>{const finalPart=prompt('Final Part No.');if(!finalPart)return;const title=prompt('ชื่อ Assembly / Product Name')||'NEW ASSEMBLY';const f={id:uid(),title,finalPart,stages:[]};flows.push(f);selected=f.id;save();render();openEditor()};
  $('bfAdd').onclick=()=>openEditor();$('bfClose').onclick=$('bfCancel').onclick=()=>{$('bfEditor').hidden=true};$('bfSelect').onchange=e=>{selected=e.target.value;render()};
  let drag=null,suppressClick=false;
  $('bfFlow').addEventListener('pointerdown',e=>{if(e.target.closest('button'))return;const node=e.target.closest('[data-node]'),canvas=e.target.closest('.bf-canvas');if(!node||!canvas)return;const nr=node.getBoundingClientRect();drag={node,canvas,dx:e.clientX-nr.left,dy:e.clientY-nr.top,sx:e.clientX,sy:e.clientY,moved:false};node.classList.add('dragging');node.setPointerCapture?.(e.pointerId)});
  $('bfFlow').addEventListener('pointermove',e=>{if(!drag)return;const cr=drag.canvas.getBoundingClientRect();let x=e.clientX-cr.left-drag.dx,y=e.clientY-cr.top-drag.dy;x=Math.max(0,Math.min(x,drag.canvas.clientWidth-drag.node.offsetWidth));y=Math.max(42,Math.min(y,drag.canvas.clientHeight-drag.node.offsetHeight));drag.moved=drag.moved||Math.abs(e.clientX-drag.sx)>4||Math.abs(e.clientY-drag.sy)>4;drag.node.style.left=x+'px';drag.node.style.top=y+'px';drawGraph()});
  $('bfFlow').addEventListener('pointerup',()=>{if(!drag)return;drag.node.classList.remove('dragging');if(drag.moved){flow().layout=flow().layout||{};flow().layout[drag.node.dataset.node]={x:parseFloat(drag.node.style.left),y:parseFloat(drag.node.style.top)};save();suppressClick=true;setTimeout(()=>suppressClick=false,80)}drag=null});
  $('bfFlow').onclick=e=>{if(suppressClick)return;const edit=e.target.closest('[data-edit-stage]');if(edit)return openEditor(edit.dataset.editStage);const del=e.target.closest('[data-delete-stage]');if(del&&confirm('ลบขั้นตอนนี้หรือไม่?')){flow().stages=flow().stages.filter(s=>s.id!==del.dataset.deleteStage);save();render();return}const card=e.target.closest('[data-part]');if(!card||card.dataset.part.startsWith('inherited-'))return;const p=flow().stages.flatMap(s=>s.inputs||[]).find(x=>x.id===card.dataset.part);if(!p)return;$('bfDetail').innerHTML=`<div class="bf-detail-card"><div class="panel-head"><div><h2>${esc(p.partNo)}</h2><p>${esc(p.name||'COMPONENT')}</p></div><button class="btn ghost" id="bfDetailClose">ปิด</button></div><p><strong>แหล่งที่มา:</strong> ${p.source==='Supplier'?'รับจาก Supplier':'ผลิตเอง (In-house)'}</p><h3>Process ที่ผ่านมาก่อนประกอบ</h3>${(p.processes||[]).map(x=>`<div class="bf-process-row"><strong>${esc(x.name)}</strong><span>${x.steps} Step</span></div>`).join('')||'<p class="muted">ไม่มี Process ภายใน / รับเข้าจาก Supplier</p>'}</div>`;$('bfDetail').hidden=false;$('bfDetailClose').onclick=()=>{$('bfDetail').hidden=true}};
  refreshSelect();render();
})();
